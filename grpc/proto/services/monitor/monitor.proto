syntax = "proto3";
package tezedge.monitor;

// Imports
import "monitor_hello.proto";

service Monitor {
    // Our SayHello rpc accepts HelloRequests and returns HelloReplies
    rpc SayHello (HelloRequest) returns (HelloReply);
}