[package]
name = "tezos-sys"
version = "1.16.1"
authors = ["Bruno Deferrari <bruno.deferrari@simplestaking.com>"]
edition = "2021"
links = "tezos"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
# NOTE: should not have any local dependencies, this crate is just for linking libtezos.so

[build-dependencies]
colored = "2.0"
fs_extra = "1.1"
hex = "0.4"
os_type = "2.2"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
sha2 = "0.9"
